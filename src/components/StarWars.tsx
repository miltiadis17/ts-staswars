import {starWarsInfo} from "../utils/constants.ts";

const StarWars = () => {
    return (
        <div className={'farGalaxy'}>
            {starWarsInfo}
        </div>
    );
};

export default StarWars;